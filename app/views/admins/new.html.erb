<% if current_user.try(:admin?)%>
    <%= form_for(@admin, :url => {:action => 'create'}) do |a| %>
        <%= a.label :email %>
        <%= a.text_field :email %>
        <%= a.submit "Create Admin" %>
    <% end %>
<% else %>
    <h1>You do not have access to this page.</h1>
    <p>You may need to <a href="/users/sign_in">log in</a></p>
<% end %>